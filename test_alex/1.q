# program de test

function max(x:int, y:int):int
    if(x<y)
        return y;
        else
        return x;
        end
    end

var i:int;
var j:real;
var k:str;
i=0;    # iterator
while(i<10)
    puti(max(i,5, 2.0));
    i=i+1;
    end

puts("PI=");
putr(3.14159);
